# 2026-09-12

## Sesión recuperada de 2026-09-11
- Copié archivos de sesión anterior (2026-09-11-0311) a esta sesión
- Exploración de modelos matemáticos ancestrales + 2 experimentos (calendario + ternario IoT)
- Todo verificado y subido a GitHub

## Subida a GitHub
- Repo: https://github.com/Astrid3333/Ternario-ancestral-sensores.git
- Token classic con scope `repo` necesario para push
- README actualizado con 6 casos de uso + documentación técnica

## Experimento: Centro de datos nodal ternario
- Run: `output/experiment-suite/nodo-ternario-datacenter/2026-09-12_021819/`
- 7 métodos comparados: binario, gzip, LZ4, ternario raw, ternario residual, ternario+quipu, ternario+ZSTD
- Resultados medidos:
  - Ternario raw: 8x compresión, 80.2% ahorro energético
  - Ternario residual: 2x compresión, 20.6% ahorro
  - Quipu: 100% errores detectados, 28.6% sobrecarga
- 4 figuras PDF/SVG publication-grade
- Código Python completo verificado

## Experimento: Compresión ancestral extendida
- Run: `output/experiment-suite/compresion-ancestral-extendida/2026-09-12_025659/`
- Sistemas comparados: Maya (base 20), Persa (base 33), Babilónico (base 60)
- Resultados teóricos + implementación:
  - Maya: 2.72 trits por dígito, 63% ahorro
  - Persa: 3.15 trits por dígito, 68% ahorro
  - Babilónico: 3.91 trits por dígito, 74% ahorro
- **Calidad verificada:** TODAS las codificaciones decodifican EXACTAMENTE (0 errores)
- 4 figuras PDF/SVG

## Optimización total de librería Arduino
- 3 modos disponibles: Lite (100B RAM), Original (500B), Babilónico (300B)
- Bit-packing: 3 trits en 5 bits (83% ahorro)
- Lookup tables para operaciones frecuentes
- Compatible con Tiny Core (16MB), BusyBox (1MB), Alpine (5MB)
- Compatible con ATtiny85 (512B RAM) en modo Lite
- Documentación completa: COMPATIBILITY.md

## Lecciones
- La compresión ancestral de mayor radix (base 60) supera a menor radix (base 20)
- La decodificación es biyectiva → 0 errores garantizados
- Implementación actual necesita bit-packing para compresión real (1 byte/dígito es ineficiente)
- Para datos de 3 estados, ternario nativo + babilónico es la combinación óptima
- El modo Lite funciona en cualquier sistema con >100 bytes de RAM

## Code Review + Nivel 2 del Kernel

### Bugs corregidos (10 encontrados, 10 corregidos)
1. `outb`/`inb` no declaradas → inline assembly en `ternary.h`
2. `mem_alloc()` retornaba `int8_t` con valor -1 → cambiado a `int16_t`
3. `select_next()` podía retornar PID 0 inválido → flag `found` agregado
4. `sched_kill()` no liberaba memoria → ahora llama `mem_free()`
5. `fs_init()` nombre "/" mal inicializado → fix con `strcpy_t`
6. `mem_get_base()` sin validación de block_idx
7. `cmd_buf[16]` overflow potencial → aumentado a 64 bytes
8. VGA sin scroll → implementado scroll automático
9. Keyboard driver incompleto → scancode set 1 completo (128 teclas)
10. Makefile: boot.asm compilaba con `-f elf32` en vez de `-f bin`

### Nivel 2 implementado
- Terminal interactiva con prompt coloreado (ternary@mayan$)
- Shell con 12 comandos: help, ps, mem, fs, cal, fork, kill, echo, clear, trit, b60, halt
- Conversor ternario: `trit <n>` muestra representación {-1,0,+1}
- Conversor babilónico: `b60 <n>` muestra dirección base 60
- Memory status con block map visual (colores por tipo)
- Process list con estado, prioridad ternaria, CPU cycles
- Archivos: `fs` muestra Quipu filesystem
- Calendario Maya: `cal` muestra Tzolkin/Haab/system load

### libc ternaria mínima
- `strlen_t`, `strcmp_t`, `strncmp_t`, `strcpy_t`, `strcat_t`
- `memset_t`, `memcpy_t`, `parse_num`, `num_to_str`

### Commits
- `45adbab` - kernel v2: code review fixes + nivel 2 (terminal + shell)
- Push exitoso a GitHub

### Lecciones
- Siempre declarar `outb`/`inb` antes de usar en kernel C
- Retornar tipos con signo para errores (-1) en funciones que retornan índices
- El scroll de VGA es esencial para terminal usable
- Keyboard driver completo requiere tabla de 128 scancodes
- Makefile: boot sector debe ser `-f bin`, kernel objects `-f elf32`

### Redirection Fix (05:15)
**Problem:** `echo hola > file` created empty file. `cat file` showed nothing.
**Root cause:** Builtins ran in parent process; `dup2()` doesn't affect `printf` buffering.
**Fix:** All commands fork (child runs builtin, parent waits). `cmd_echo` uses `write()` instead of `printf`. `cmd_cat` uses raw `read()`/`write()`.
**Result:** `echo > file`, `echo >> file`, `cat`, `ls | head -3 | wc -l`, `alias g=ls` + `g / | head -2` all work correctly.
**Commit:** `1e77547` — pushed.

### Alias Expansion Fix
**Problem:** `alias ll=ls -la` lost `-la` due to tokenizer splitting on spaces.
**Fix:** `alias`/`unalias` handled in `parse_and_run` before tokenization. Alias expansion uses `strchr(line, ' ')` to find first word boundary, expands, and passes full expanded string to pipe/chain splitting.
**Result:** `alias ll=ls -la` + `ll /tmp | head -5` works.

### Remaining known issues
- `&&` doesn't check exit codes (recursive `parse_and_run` always runs both sides) — low priority
- `jobs`/`bg` partially implemented (jobs list but bg doesn't actually resume)

### && || Fix + New Commands (05:30)
**Problem:** `&&` ran both sides regardless of exit status. `||` same.
**Fix:** `run_single` returns exit status from `waitpid`. `parse_and_run` returns `int`. `&&` checks `left == 0`, `||` checks `left != 0`.
**Root cause:** All builtins returned `void`; child always `_exit(0)`. Fixed `cmd_ls` to return `int`, child dispatch uses `builtin_rc`.
**New commands:** `mkdir`, `mv`, `cp`, `bg`.
**Size:** 60KB.
**Commit:** `20fc222` — pushed.
